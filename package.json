{
  "name": "skiplink.js",
  "version": "0.1.0",
  "author": "CommunicateHealth",
  "description": "Skip links + a little headroom and focus",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/communicatehealth/skiplink.js.git"
  },
  "scripts": {
    "eslint": "eslint **/*.js --fix",
    "lint": "npm run prettier && npm run eslint",
    "prettier": "prettier --write **/*.{js,json,md,html}"
  },
  "devDependencies": {
    "eslint": "7.23.0",
    "prettier": "2.2.1"
  }
}
